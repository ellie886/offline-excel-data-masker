# Security Policy

## Reporting a vulnerability

请通过 GitHub 的私密安全报告功能提交安全问题，不要在公开 Issue 中附带真实工作簿、个人信息、客户资料、访问令牌或其他敏感数据。

安全报告建议包含：

- 受影响版本或提交；
- 可复现的最小步骤；
- 使用虚构数据制作的测试文件；
- 预期行为与实际行为；
- 可能造成的数据泄露或文件损坏影响。

## Current security boundary

当前版本的核心处理流程可以断网运行，并且不会主动调用云端 API。已确认的普通单元格使用 OOXML 定点修改；无引用敏感共享字符串会被删除，公式缓存会被清理，且输出前会扫描解压后的 XML/RELS。

对于下列尚未能可靠清理的组件，工具会失败关闭并阻止导出，而不是仅显示警告：

- 透视表/图表缓存、文本框、形状、批注、嵌入对象和附件；
- 外部链接、数据连接、Power Query、数据模型和自定义 XML；
- 宏、数字签名、加密/密码保护及无法安全解析的组件；
- 未扫描的隐藏工作表和任何 veryHidden 工作表。

以下内容仍属于运行环境风险或未覆盖范围：

- 浏览器扩展、本机恶意软件、系统交换空间及崩溃转储；
- 云同步或自动备份的下载目录；
- 恶意构造工作簿对第三方解析库的攻击；
- Microsoft Excel、LibreOffice 等不同引擎间的兼容性差异。

不要把测试通过等同于安全认证。处理真实敏感数据前，应在隔离环境中完成组织内部的代码审查、恶意文件测试和输出文件人工复核。
